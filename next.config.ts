import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["https://7ee300bba654.ngrok-free.app"],
};

export default nextConfig;

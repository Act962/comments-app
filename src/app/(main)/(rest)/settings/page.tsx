import {
  SettingsContainer,
  SettingsContent,
} from "@/features/settings/components/settings";

export default function Settings() {
  return (
    <SettingsContainer>
      <SettingsContent />
    </SettingsContainer>
  );
}
